export interface CompiledReportSource {
    init?(): Promise<void>;
    readFile(relativePath: string): Promise<string | null>;
    listFiles(prefix: string): Promise<string[]>;
}
export declare function stripFileProtocol(input: string): string;
export declare class FileReportSource implements CompiledReportSource {
    #private;
    constructor(dir: string);
    readFile(relativePath: string): Promise<string | null>;
    listFiles(prefix: string): Promise<string[]>;
}
export declare class UrlReportSource implements CompiledReportSource {
    #private;
    constructor(baseUrl: string);
    readFile(relativePath: string): Promise<string | null>;
    listFiles(prefix: string): Promise<string[]>;
}
export declare class HtmlReportSource implements CompiledReportSource {
    #private;
    constructor(fileOrUrl: string, prefetchedHtml?: string);
    init(): Promise<void>;
    readFile(relativePath: string): Promise<string | null>;
    listFiles(prefix: string): Promise<string[]>;
}
export declare function containsHtmlDataCalls(content: string): boolean;
//# sourceMappingURL=CompiledReportSource.d.ts.map